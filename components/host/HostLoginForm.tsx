'use client';
import {FormEvent,useState} from 'react';
import {createBrowserClient} from '@supabase/ssr';
import {useRouter} from 'next/navigation';
import {useLocale} from '@/components/i18n/LocaleProvider';
import styles from './HostLoginForm.module.css';
export function HostLoginForm(){const router=useRouter();const{locale}=useLocale();const en=locale==='en';const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);const s=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!),{error}=await s.auth.signInWithPassword({email,password});if(error){setError(en?'Your email or password is incorrect.':'อีเมลหรือรหัสผ่านไม่ถูกต้องครับ');setLoading(false);return}router.push('/trips');router.refresh()}return <div className={styles.shell}><form onSubmit={submit} className={styles.form}><span className={styles.kicker}>GEPPAO ACCOUNT</span><h1>{en?'Welcome back':'เข้าสู่ระบบ GepPao'}</h1><p>{en?'One account for your trips, business profile, and local-data work.':'บัญชีเดียวสำหรับทริป ข้อมูลธุรกิจ และงานดูแล Local Data ตามสิทธิ์ของคุณ'}</p><div className={styles.fields}><div className="form-field"><label htmlFor="email">{en?'Email':'อีเมล'}</label><input id="email" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></div><div className="form-field"><label htmlFor="password">{en?'Password':'รหัสผ่าน'}</label><input id="password" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/></div></div>{error&&<p className="form-error" role="alert">{error}</p>}<button className="btn btn-primary" disabled={loading}>{loading?(en?'Signing in…':'กำลังเข้าสู่ระบบ...'):(en?'Sign in':'เข้าสู่ระบบ')}</button><p className={styles.note}>{en?'You will return to your saved trips after sign-in. Business and admin tools remain available to accounts with permission.':'หลังเข้าสู่ระบบ คุณจะกลับไปที่ทริปของฉัน ส่วนเครื่องมือธุรกิจและ Admin จะแสดงตามสิทธิ์ของบัญชี'}</p></form></div>}
