import Link from 'next/link';
import { HOST_PACKAGES } from '@/lib/marketplace/packages';
export function HostCTA(){return <section className="section"><div className="container"><div className="section-head"><div><h2>สำหรับเจ้าของที่พัก</h2><p className="muted">ลงประกาศฟรีเพื่อเริ่มสร้าง demand จากนักเดินทางของ GepPao</p></div></div><div className="host-grid">{Object.entries(HOST_PACKAGES).map(([key,p])=><div className={`plan-card ${key==='premium'?'premium':''}`} key={key}><h3>{p.name}{key==='premium'?' · Recommended':''}</h3><div className="plan-price">฿{p.price}<span style={{fontSize:'.9rem',fontWeight:400}}>/เดือน</span></div><ul className="list">{p.features.map(f=><li key={f}>{f}</li>)}</ul><Link className={`btn ${key==='premium'?'btn-sage':'btn-primary'}`} href="/host">ลงทะเบียนที่พัก</Link></div>)}</div></div></section>}
