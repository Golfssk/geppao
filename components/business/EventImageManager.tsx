'use client';import {ChangeEvent,useState} from 'react';export function EventImageManager({eventId,initialImages}:{eventId:string;initialImages:any[]}){const[images,setImages]=useState(initialImages),[msg,setMsg]=useState('');async function upload(e:ChangeEvent<HTMLInputElement>){for(const file of Array.from(e.target.files??[])){const f=new FormData();f.append('file',file);const r=await fetch(`/api/business/events/${eventId}/images`,{method:'POST',body:f}),x=await r.json();if(r.ok)setImages(v=>[...v,x]);else setMsg(x.error)}e.target.value=''}async function remove(id:string){const r=await fetch(`/api/business/events/${eventId}/images`,{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({imageId:id})});if(r.ok)setImages(v=>v.filter(x=>x.id!==id))}return <section className="dashboard-card"><div className="section-head"><div><h2>รูป Event</h2><p className="muted">JPG, PNG หรือ WebP ไม่เกิน 8 MB</p></div><label className="btn btn-primary">เพิ่มรูป<input hidden multiple type="file" accept="image/jpeg,image/png,image/webp" onChange={upload}/></label></div><div className="place-image-grid">{images.map(i=><article key={i.id}><img src={i.image_url} alt="รูป Event"/><div className="image-actions"><button className="btn" onClick={()=>remove(i.id)}>ลบ</button></div></article>)}</div>{msg&&<p className="form-error">{msg}</p>}</section>}
