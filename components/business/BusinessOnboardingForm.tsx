'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';export function BusinessOnboardingForm({email}:{email:string}){const router=useRouter();const[name,setName]=useState(''),[phone,setPhone]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/business/onboarding',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,phone})});const x=await r.json();if(!r.ok){setError(x.error||'สร้างบัญชีไม่สำเร็จ');setLoading(false);return}router.replace('/business/places/new');router.refresh()}return <form className="card business-form" onSubmit={submit}><div className="card-body"><h2>สร้างโปรไฟล์ธุรกิจฟรี</h2><p className="muted">ใช้บัญชี {email} เป็นเจ้าของธุรกิจ คุณแก้ไขข้อมูลได้ภายหลัง</p><label>ชื่อธุรกิจหรือสถานที่ *<input required minLength={2} maxLength={120} value={name} onChange={e=>setName(e.target.value)}/></label><label>เบอร์โทร<input type="tel" maxLength={30} autoComplete="tel" value={phone} onChange={e=>setPhone(e.target.value)}/></label>{error&&<p role="alert" className="form-error">{error}</p>}<div className="form-actions"><button className="btn btn-primary" disabled={loading}>{loading?'กำลังสร้าง…':'เริ่มลงประกาศฟรี'}</button></div></div></form>}
