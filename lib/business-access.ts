import {createClient} from '@/lib/supabase/server';
export async function getBusinessContext(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return{supabase,user:null,business:null};const {data:membership}=await supabase.from('business_members').select('business_id,role,businesses(id,name,status)').eq('user_id',user.id).in('role',['owner','admin','editor']).limit(1).maybeSingle();const related=membership?.businesses;const business=Array.isArray(related)?related[0]:related;return{supabase,user,business:business?{id:business.id,name:business.name,status:business.status,role:membership!.role}:null};}
