export default function Admin(){return <main className="page"><div className="container section"><h1>GepPao Admin</h1><p className="muted">MVP dashboard — connect to Supabase analytics in the next iteration.</p><div className="dashboard-grid" style={{marginTop:24}}>{[['Active Listings','328'],['Premium Hosts','74'],['AI Trips','2,481'],['Leads','612']].map(([k,v])=><div className="stat" key={k}><div className="muted">{k}</div><div className="stat-value">{v}</div></div>)}</div></div></main>}
