'use client';import {useEffect} from 'react';export default function ErrorBoundary({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error('GepPao page error',error)},[error]);return <main className="page"><div className="container section"><div className="setup-notice"><h1>หน้านี้เกิดข้อผิดพลาด</h1><p>ข้อมูลของคุณยังไม่ถูกลบ ลองโหลดหน้านี้ใหม่อีกครั้ง</p><button className="btn btn-primary" onClick={reset}>ลองอีกครั้ง</button>{error.digest&&<p className="muted">รหัสเหตุการณ์: {error.digest}</p>}</div></div></main>}
