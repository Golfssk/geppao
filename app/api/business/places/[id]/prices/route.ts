import {NextResponse} from 'next/server';import {getBusinessContext} from '@/lib/business-access';
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase,user,business}=await getBusinessContext();if(!user)return NextResponse.json({error:'กรุณาเข้าสู่ระบบ'},{status:401});if(!business)return NextResponse.json({error:'ไม่พบบัญชีธุรกิจ'},{status:403});const{items}=await request.json();if(!Array.isArray(items)||items.some(item=>!item.label||!item.unit||Number(item.amountMin)<0))return NextResponse.json({error:'ข้อมูลราคาไม่ถูกต้อง'},{status:400});const{error}=await supabase.rpc('replace_place_prices',{target_place_id:id,items});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({success:true});}
