import {NextResponse} from 'next/server';
import {getBusinessContext} from '@/lib/business-access';
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase,user,business}=await getBusinessContext();if(!user)return NextResponse.json({error:'กรุณาเข้าสู่ระบบ'},{status:401});if(!business)return NextResponse.json({error:'ไม่พบบัญชีธุรกิจ'},{status:403});const body=await request.json();const allowed=['draft','pending','archived'];const status=allowed.includes(body.publicationStatus)?body.publicationStatus:undefined;const updates:Record<string,unknown>={updated_at:new Date().toISOString()};if(status)updates.publication_status=status;if(typeof body.name==='string'&&body.name.trim())updates.name=body.name.trim();if(typeof body.description==='string')updates.description=body.description.trim()||null;const{data,error}=await supabase.from('places').update(updates).eq('id',id).eq('business_id',business.id).select('id,publication_status').maybeSingle();if(error)return NextResponse.json({error:error.message},{status:500});if(!data)return NextResponse.json({error:'ไม่พบ Place หรือไม่มีสิทธิ์'},{status:404});return NextResponse.json(data);}
