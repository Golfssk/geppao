import {redirect,notFound} from 'next/navigation';import {getBusinessContext} from '@/lib/business-access';import {EventEditor} from '@/components/business/EventEditor';import {EventImageManager} from '@/components/business/EventImageManager';export const dynamic='force-dynamic';export default async function EventPage({params}:{params:Promise<{id:string}>}){const{id}=await params,{supabase,user,business}=await getBusinessContext();if(!user)redirect('/host/login');if(!business)redirect('/business/places');const{data:event}=await supabase.from('events').select('*,event_schedules(id,starts_at,ends_at,status),event_images(id,image_url,is_cover,sort_order,alt_text)').eq('id',id).eq('business_id',business.id).maybeSingle();if(!event)notFound();return <main className="page"><div className="container section"><p className="eyebrow">EVENT MANAGEMENT</p><h1>{event.name}</h1><div className="editor-stack"><EventEditor event={event}/><EventImageManager eventId={event.id} initialImages={event.event_images??[]}/></div></div></main>}
