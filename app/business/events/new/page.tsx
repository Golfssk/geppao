'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';export default function NewEvent(){const router=useRouter(),[error,setError]=useState('');async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const b=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/business/events',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}),x=await r.json();if(!r.ok)return setError(x.error);router.push(`/business/events/${x.id}`)}return <main className="page"><div className="container section"><p className="eyebrow">NEW EVENT</p><h1>เพิ่ม Event</h1><form className="dashboard-card business-form" onSubmit={save}><label>ชื่อ Event<input name="name" required/></label><label>สถานที่ชั่วคราว<input name="temporaryVenueName"/></label><label>ที่อยู่<textarea name="address"/></label><label>รายละเอียด<textarea name="description" rows={5}/></label><button className="btn btn-primary">สร้าง Draft Event</button>{error&&<p className="form-error">{error}</p>}</form></div></main>}
