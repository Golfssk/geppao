import {redirect} from 'next/navigation';import {getBusinessContext} from '@/lib/business-access';import {BusinessOnboardingForm} from '@/components/business/BusinessOnboardingForm';export default async function BusinessOnboarding(){const{user,business}=await getBusinessContext();if(!user)redirect('/host/login?next=%2Fbusiness%2Fonboarding');if(business)redirect('/business/places');return <main className="page"><div className="container section"><p className="eyebrow">FREE OWNER ONBOARDING</p><h1>เริ่มต้นกับ GepPao Business</h1><p className="muted">สร้างโปรไฟล์ฟรี แล้วนำเข้าข้อมูลจากเว็บไซต์หรือ Facebook เพื่อลดเวลาการกรอกข้อมูล</p><BusinessOnboardingForm email={user.email||''}/></div></main>}
