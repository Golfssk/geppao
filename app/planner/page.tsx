'use client';
import { useSearchParams } from 'next/navigation';
import { useState } from 'react';
export default function Planner(){const params=useSearchParams();const [input,setInput]=useState(params.get('q')||'');const [loading,setLoading]=useState(false);const [result,setResult]=useState<any>(null);async function run(){if(!input.trim())return;setLoading(true);setResult(null);try{const r=await fetch('/api/ai/trip',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({input})});const j=await r.json();setResult(j);}finally{setLoading(false)}}return <main className="page"><div className="container section"><h1>AI Trip Planner</h1><p className="muted">เล่าทริปในภาษาของคุณ แล้ว GepPao จะช่วยจัดที่พัก + จุดแฮงเอ้าท์ + itinerary</p><div className="planner-box" style={{marginTop:24}}><textarea value={input} onChange={e=>setInput(e.target.value)} placeholder="เช่น ไปกับเพื่อน 8 คน สายปาร์ตี้ มีหมา งบ 2,000 บาท/คน 2 วัน 1 คืน"/><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginTop:12,gap:10}}><span className="muted" style={{fontSize:'.82rem'}}>AI จะใช้เฉพาะข้อมูลที่มาจากระบบ GepPao</span><button className="btn btn-rust" disabled={loading} onClick={run}>{loading?'กำลังจัดทริป...':'ให้ AI จัดทริป'}</button></div></div>{result&&<div className="planner-box" style={{marginTop:24}}><h2>ทริปของคุณ</h2><pre style={{whiteSpace:'pre-wrap'}}>{JSON.stringify(result,null,2)}</pre></div>}</div></main>}
