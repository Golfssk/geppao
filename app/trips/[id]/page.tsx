import {redirect,notFound} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {TripWorkspace} from '@/components/trips/TripWorkspace';export const dynamic='force-dynamic';export default async function TripPage({params}:{params:Promise<{id:string}>}){const{id}=await params,s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect('/host/login');const[{data:trip},{data:places},{data:events}]=await Promise.all([s.from('trips').select('*,trip_days(*,trip_items(*,places(id,name,slug,place_type,address,latitude,longitude,google_maps_url),events(id,name,slug,address,temporary_venue_name,latitude,longitude)))').eq('id',id).order('day_number',{referencedTable:'trip_days'}).maybeSingle(),s.from('places').select('id,name,place_type,price_items(amount_min)').eq('publication_status','published'),s.from('events').select('id,name,price_items(amount_min)').eq('publication_status','published')]);if(!trip)notFound();const candidates=[...(places??[]).map((x:any)=>({key:`p-${x.id}`,kind:'place',id:x.id,name:x.name,type:x.place_type,cost:Number(x.price_items?.[0]?.amount_min||0)})),...(events??[]).map((x:any)=>({key:`e-${x.id}`,kind:'event',id:x.id,name:x.name,type:'event',cost:Number(x.price_items?.[0]?.amount_min||0)}))];return <main className="page"><div className="container section"><p className="eyebrow">TRIP WORKSPACE</p><h1>{trip.title}</h1><p className="muted">{trip.start_date} – {trip.end_date} · {trip.travelers} คน · {trip.status}</p><TripWorkspace trip={trip} candidates={candidates}/></div></main>}
