'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';export default function NewTrip(){const router=useRouter(),[error,setError]=useState('');async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const b:any=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/trips',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)}),x=await r.json();if(!r.ok)return setError(x.error);router.push(`/trips/${x.id}`)}return <main className="page"><div className="container section"><p className="eyebrow">NEW TRIP</p><h1>สร้างทริป</h1><form className="dashboard-card business-form" onSubmit={save}><label>ชื่อทริป<input name="title" required placeholder="เขาใหญ่กับครอบครัว"/></label><div className="form-grid"><label>วันเริ่ม<input name="startDate" type="date"/></label><label>วันสิ้นสุด<input name="endDate" type="date"/></label><label>จำนวนคน<input name="travelers" type="number" min="1" defaultValue="2"/></label><label>งบรวม<input name="budgetTotal" type="number" min="0"/></label></div><button className="btn btn-primary">สร้างทริป</button>{error&&<p className="form-error">{error}</p>}</form></div></main>}
