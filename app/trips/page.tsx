import Link from 'next/link';import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';export const dynamic='force-dynamic';export default async function Trips(){const s=await createClient(),{data:{user}}=await s.auth.getUser();if(!user)redirect('/host/login');const{data}=await s.from('trips').select('id,title,start_date,end_date,travelers,budget_total,currency,status,trip_days(id)').order('updated_at',{ascending:false});return <main className="page"><div className="container section"><div className="section-head"><div><p className="eyebrow">TRIP WORKSPACE</p><h1>ทริปของฉัน</h1></div><Link className="btn btn-primary" href="/trips/new">+ สร้างทริป</Link></div><div className="grid">{(data??[]).map(t=><article className="card" key={t.id}><div className="card-body"><h2>{t.title}</h2><p className="muted">{t.start_date||'ยังไม่ระบุวัน'} – {t.end_date||'ยังไม่ระบุวัน'} · {t.travelers} คน</p><p>{t.budget_total?`งบ ${Number(t.budget_total).toLocaleString()} ${t.currency}`:'ยังไม่ระบุงบ'} · {t.status}</p><Link className="btn" href={`/trips/${t.id}`}>เปิดทริป</Link></div></article>)}</div></div></main>}
